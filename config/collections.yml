modules:
  - name: "NetworkExplorator"
    file_path: "modules/enumeration/network_exploration.go"
    attack_id: "enum01"
    dependsOn : ""

  - name: "SMBCoece"
    attack_id: "exploit01"
    dependsOn:
      - "enum01"
      -
  - name: "DNSExploration"
    attack_id: "enum02"
    dependsOn:
      - "enum01"
      - "exploit01"


